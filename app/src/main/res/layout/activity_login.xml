<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".loginregister.LoginActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="16dp"
        tools:context=".loginregister.LoginActivity"
        tools:layout_editor_absoluteX="0dp"
        tools:layout_editor_absoluteY="-61dp">

    <!-- ImageView para la imagen (ahora directamente en el LinearLayout vertical) -->
        <ImageView
            android:id="@+id/imgLogoApp"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="8dp"
            android:contentDescription="@string/logo_de_la_app"
            android:src="@mipmap/logo_petcare_imagen" />

        <EditText
            android:id="@+id/campoEmail"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:autofillHints="emailAddress"
            android:fontFamily="@font/abeezee_regular"
            android:hint="@string/email"
            android:inputType="textEmailAddress"
            android:minHeight="48dp" />

    <EditText
        android:id="@+id/campoContrasena"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginBottom="8dp"
        android:autofillHints="password"
        android:hint="@string/contrasenia"
        android:inputType="textPassword"
        android:fontFamily="@font/abeezee_regular"/>

    <!-- Texto "Olvidé mi contraseña" que actúa como botón -->

        <TextView
            android:id="@+id/txtRecuperarContrasena"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:layout_gravity="end"
            android:layout_marginBottom="24dp"
            android:background="?android:attr/selectableItemBackground"
            android:clickable="true"
            android:focusable="true"
            android:fontFamily="@font/abeezee_regular"
            android:gravity="end"
            android:text="@string/contrasenia_olvidada"
            android:textColor="@color/design_default_color_primary" />

    <Button
        android:id="@+id/btnLogin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:backgroundTint="@color/button_color"
        android:text="@string/iniciar_sesion"
        android:fontFamily="@font/abeezee_regular"/> <!-- Espacio entre los botones -->

    <Button
        android:id="@+id/btnRegistrar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:backgroundTint="@color/button_color"
        android:text="@string/registrarse"
        android:fontFamily="@font/abeezee_regular"/>

</LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>