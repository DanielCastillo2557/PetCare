<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="8dp"
    android:paddingEnd="60dp"
    android:paddingTop="4dp"
    android:paddingBottom="4dp"
    android:clipToPadding="false"> <!-- Permite que las sombras (si las hay) se dibujen fuera del padding -->

    <!-- Opcional: Nombre del emisor (si quieres mostrarlo en chats grupales o identificar al otro usuario) -->
    <!--
    <TextView
        android:id="@+id/textViewSenderName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="start"
        android:textSize="12sp"
        android:textColor="@android:color/holo_blue_dark"
        android:layout_marginStart="12dp"
        android:layout_marginBottom="2dp"
        tools:text="Nombre Emisor" />
    -->

<TextView
android:id="@+id/textViewMessageContent"
android:layout_width="wrap_content"
android:layout_height="wrap_content"
android:layout_gravity="start"
android:background="@drawable/bg_burbuja_de_mensaje_recibido"
    android:paddingStart="12dp"
    android:paddingEnd="12dp"
    android:paddingTop="8dp"
    android:paddingBottom="8dp"
    android:textColor="@android:color/black"
    android:textSize="16sp"
    tools:text="Este es un mensaje recibido de prueba para ver cómo se ve." />

<TextView
android:id="@+id/textViewMessageTimestamp"
android:layout_width="wrap_content"
android:layout_height="wrap_content"
android:layout_gravity="start"
android:layout_marginTop="2dp"
android:textSize="10sp"
android:textColor="@android:color/darker_gray"
tools:text="10:02 AM"
tools:ignore="SmallSp" />

    </LinearLayout>